<?xml version="1.0"?>
<ruleset name="Jobberwocky">
    <description>PHP CodeSniffer configuration for Jobberwocky project</description>

    <!-- What to scan -->
    <file>src</file>
    <file>tests</file>

    <!-- How to scan -->
    <arg name="colors"/>
    <arg name="parallel" value="80"/>

    <!-- Rules: Check against the PSR-12 standard -->
    <rule ref="PSR12"/>

    <!-- Exclude some rules -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="140"/>
        </properties>
    </rule>

    <!-- Exclude paths -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/var/*</exclude-pattern>
</ruleset>


